---
title: "[LeetCode] 1. Two Sum (투 포인터/해시맵)"
date: "2026-03-20"
description: "배열 내 두 수의 합이 타겟이 되는 인덱스를 찾는 문제. 해시맵을 사용해 시간 복잡도를 O(N)으로 줄이는 방법을 알아봅니다."
tags: ["알고리즘", "LeetCode", "HashMap"]
---

# 1. Two Sum

배열 내의 두 원소의 합이 `target`이 되는 인덱스 두 개를 찾아내는 가장 기본적인 문제입니다.

## 문제 접근 방법

가장 단순한 방법은 이중 반복문을 돌며 모든 쌍을 확인하는 `O(N^2)` 방식이지만, 배열의 크기가 커지면 **시간 초과**가 발생할 수 있습니다. 
이를 해결하기 위해 **해시맵(HashMap)**을 사용하여 `O(N)` 시간에 해결할 수 있습니다.

## 해시맵을 활용한 풀이 (O(N))

현재 원소 `num`을 순회하면서, `target - num` 값이 이미 해시맵에 존재하는지 확인합니다. 
존재한다면 그 인덱스와 현재 인덱스를 반환하고, 없다면 현재 원소와 인덱스를 해시맵에 저장합니다.

```js
/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var twoSum = function(nums, target) {
    const map = new Map();
    
    for (let i = 0; i < nums.length; i++) {
        const complement = target - nums[i];
        
        if (map.has(complement)) {
            return [map.get(complement), i];
        }
        map.set(nums[i], i);
    }
    
    return [];
};
```

### 복잡도 분석
* **시간 복잡도**: `O(N)` - 배열을 단 한 번만 순회합니다.
* **공간 복잡도**: `O(N)` - 해시맵에 최대 N개의 원소가 저장됩니다.
